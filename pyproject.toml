[tool.poetry]
name = "event"
version = "0.1.0"
description = "Event garuantee to gabon"
authors = ["kholeur9"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^5.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
